# ── Dependencies ──
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# ── Testing ──
/coverage

# ── Next.js ──
/.next/
/out/

# ── Production ──
/build

# ── Env & secrets ──
.env
.env.*
!.env.example
!.env.*.example

# ── Debug logs ──
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ── Misc / OS ──
.DS_Store
*.pem

# ── Vercel ──
.vercel

# ── TypeScript ──
*.tsbuildinfo
next-env.d.ts

# ── Claude local settings ──
.claude/settings.local.json
